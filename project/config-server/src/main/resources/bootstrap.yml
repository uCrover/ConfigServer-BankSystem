server:
  port: 8090

spring:
  security:
    user :
      name: crover
      password : cr0v3r
  cloud:
    config:
      server:
        git:
          uri: https://github.com/uCrover/nttdata-config.git
        #searchPaths: / (en caso esté en la raíz)
        #default-label: "main" (especificamos la rama en donde está)
        #en caso sea privado añadir
        #username: xxxxxx
        #password: xxxxxx
